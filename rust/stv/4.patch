--- lib.rs	2023-01-09 21:03:20.692716983 +0000
+++ lib.rs_	2023-01-09 21:04:07.449794788 +0000
@@ -1,3 +1,4 @@
+use rayon::prelude::*;
 use rayon::slice::ParallelSliceMut;
 
 use crate::*;
@@ -27,7 +28,7 @@
                 a.partial_cmp(b).expect("partial_cmp found NaN")
             });
             let ballot: Vec<_> =
-                distances.iter().map(|(i, _)| *i).collect();
+                distances.par_iter().map(|(i, _)| *i).collect();
             StvBallot(ballot)
         })
         .collect()
